<div id="report-status">
    <% case report.status %>
    <% when "pending" %>
    <%= render "reports/status/pending", report: report %>
    <% when "processing" %>
    <%= render "reports/status/processing", report: report %>
    <% when "completed" %>
    <%= render "reports/status/completed", report: report %>
    <% when "failed" %>
    <%= render "reports/status/failed", report: report %>
    <% end %>
    
    <p class="mt-4 text-right text-sm">
        <% if report.failed? %>
        <%= link_to "Retry", regenerate_report_path(report), class: "text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-200" %>, or 
        <% end %> 
        <%= link_to "Create new report", new_report_path, class: "text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-200" %>
    </p>
</div>