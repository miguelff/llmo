<div id="report-status">
    <% case report.status %>
    <% when "pending" %>
    <%= render "reports/status/pending", report: report %>
    <% when "processing" %>
    <%= render "reports/status/processing", report: report %>
    <% when "completed" %>
    <%= render "reports/status/completed", report: report %>
    <% when "failed" %>
    <%= render "reports/status/failed", report: report %>
    <% end %>
    
    <p class="mt-4 text-right text-sm">
        <% if report.failed? %>
        <%= link_to "Retry", retry_report_path(report), class: "text-accent-600 hover:text-accent-800 dark:text-accent-400 dark:hover:text-accent-200" %>, or 
        <% end %> 
        <%= link_to "Create new report", new_report_path, class: "text-accent-600 hover:text-accent-800 dark:text-accent-400 dark:hover:text-accent-200" %>
    </p>
</div>